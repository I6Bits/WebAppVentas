@model IEnumerable<WebAppVentas.Models.Venta_Detalle_Tmp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Libro.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subtotal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Libro.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subtotal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Venta_Detalle_Tmp_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Venta_Detalle_Tmp_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Venta_Detalle_Tmp_ID })
        </td>
    </tr>
}

</table>
